<scroll-view wx:if="{{ scroll }}" scroll-x="{{ direction === 'horizontal' }}" scroll-y="{{ direction === 'vertical' }}" class="vx-class {{ ss.wrap }}">
    <slot></slot>
</scroll-view>
<view wx:else class="vx-class {{ ss.wrap }}">
    <slot></slot>
</view>
